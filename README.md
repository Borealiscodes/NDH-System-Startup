# NDH-System-Startup
NDH‑System‑Startup defines the canonical boot sequence for NDH, binding A0 → A10 → A11 → A12 through the Minimal Sequencing Anchor v1.0. It provides a stable temporal origin, prevents refresh collapse, and ensures deterministic altitude, membrane, continuity, and substrate initialization.
